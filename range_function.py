# range function is used to generate a sequence of numbers,
# a range object is an abject that stores a sequence of numbers


numbers = range(7)

for number in numbers:
    print(number)

# if two parameters are given, the range starts from the first index and stops at the number before the second index
names = range(5, 11)
for name in names:
    {
        print(name)
    }