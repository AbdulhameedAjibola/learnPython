# tuples are a type of list used to store objects, but they are immutable, meaning it cannot be changed or eiditedafter
# we use parentheses/normal brackts to define tuples instead of square brackets

numbers = ("john", "james", "joju")
